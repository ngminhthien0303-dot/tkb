# Sao chép file này thành .env rồi điền giá trị thật.

# Chuỗi kết nối Postgres (lấy từ Neon: https://neon.tech)
DATABASE_URL=postgresql://user:password@host/dbname?sslmode=require

# Chuỗi bí mật để ký token đăng nhập (đặt chuỗi ngẫu nhiên dài)
JWT_SECRET=hay-doi-thanh-chuoi-ngau-nhien-that-dai
